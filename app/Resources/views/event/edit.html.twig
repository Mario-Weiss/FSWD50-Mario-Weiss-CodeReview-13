{% extends 'base.html.twig' %}

{% block body %}
<div class="col-12 text-center text-red p-3"><h1>Edit Event</h1></div>   
<div class="col-12 card">
	<div class="card-body">
		{{form_start(form)}}
		{{form_widget(form)}}
		{{form_end(form)}}
	</div>
</div>

{% endblock %}